{
	admin off
}

# TLS is terminated by the pmp-edge ingress; this Caddy only serves HTTP
# on :80 inside the Docker network.
:80 {
	encode zstd gzip

	handle /api/* {
		reverse_proxy api:8000 {
			flush_interval -1
		}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
